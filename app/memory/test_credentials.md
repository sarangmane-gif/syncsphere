# Test Credentials — SyncSphere

This application has **no authentication** (internal intranet demo). No login is required.
All pages are publicly reachable at the app root.

- App URL: uses REACT_APP_BACKEND_URL (frontend/.env)
- Backend API base: `${REACT_APP_BACKEND_URL}/api`
- Data is seeded automatically on backend startup (announcements, leaves, feedback).
